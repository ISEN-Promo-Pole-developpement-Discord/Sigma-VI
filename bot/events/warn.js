module.exports = {
    name: "warn",
    once: false,
    execute(info) {
        /**
         * Triggered when a user gets warned
         * @param {String} info The warning
         * @returns {Promise<void>}
        */
    }
}