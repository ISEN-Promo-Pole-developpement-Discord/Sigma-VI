module.exports = {
    name: "roleCreate",
    once: false,
    execute(role) {
        /**
         * Emitted whenever a role is created.
         * @param {Role} role The role that got created
         * @event roleCreate
         * @returns {Promise<void>}
            */
    }
}