
async function createChannel(guild,user) {
    console.log(`\nomg je suis dans le serveur :${guild}\n`);
    console.log(`\n et l'utililsateur concerné est  : ${user}`);
  //  guild.channels.create(`form user : ${user}`,);
}


module.exports = {
    createChannel
}